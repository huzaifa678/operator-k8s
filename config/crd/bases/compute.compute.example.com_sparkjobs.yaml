---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: sparkjobs.compute.compute.example.com
spec:
  group: compute.compute.example.com
  names:
    kind: SparkJob
    listKind: SparkJobList
    plural: sparkjobs
    singular: sparkjob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.runningExecutors
      name: Executors
      type: integer
    - jsonPath: .status.estimatedCostUSD
      name: Cost
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SparkJob is the Schema for the sparkjobs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SparkJobSpec defines a Spark application to submit and supervise.
            properties:
              arguments:
                description: Arguments passed to the application.
                items:
                  type: string
                type: array
              checkpoint:
                description: |-
                  CheckpointSpec configures where state is persisted so a job can resume after
                  pod eviction (e.g. spot interruption).
                properties:
                  credentialsSecret:
                    description: 'Name of a Secret with keys: accessKey, secretKey,
                      endpoint.'
                    type: string
                  enabled:
                    default: false
                    type: boolean
                  interval:
                    default: 5m
                    description: How often the workload should checkpoint.
                    type: string
                  s3URI:
                    description: S3-compatible URI (e.g. s3://bucket/prefix). MinIO
                      works.
                    type: string
                type: object
              driverResources:
                description: Driver pod resource requests.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              executorResources:
                description: Executor pod resource requests.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              executors:
                default: 0
                description: |-
                  Static executor count. If 0 and ResourceHint.InputSizeMB > 0, the
                  controller auto-tunes based on input size.
                format: int32
                type: integer
              image:
                default: apache/spark:3.5.3
                description: |-
                  Container image with a Spark distribution. Must contain /opt/spark/bin/spark-submit.
                  Defaults to apache/spark:3.5.3.
                type: string
              mainApplicationFile:
                description: |-
                  JAR or .py file the driver runs. May be local:///path baked into the image,
                  or a fetchable http(s):// / s3a:// URI.
                type: string
              mainClass:
                description: Fully-qualified main class (Scala/Java only).
                type: string
              maxExecutors:
                default: 20
                format: int32
                type: integer
              minExecutors:
                default: 1
                description: Min/max bounds for the auto-tuner.
                format: int32
                type: integer
              resourceHint:
                description: |-
                  ResourceHint lets the auto-tuner make decisions without the user
                  over-specifying executor/worker counts up front.
                properties:
                  costPerHourUSD:
                    default: "0.10"
                    description: Estimated cost per hour per pod, in USD. Used for
                      status.estimatedCost.
                    type: string
                  inputSizeMB:
                    description: Estimated input size in MB. Used to compute executor
                      count.
                    format: int64
                    type: integer
                type: object
              serviceAccount:
                description: |-
                  ServiceAccount the driver runs as. If empty, the controller creates
                  `<job>-driver-sa` with the minimum RBAC Spark needs to create executor pods.
                type: string
              sparkConf:
                additionalProperties:
                  type: string
                description: Extra --conf key=value pairs passed to spark-submit.
                type: object
              spot:
                description: SpotPolicy controls how the controller schedules onto
                  preemptible nodes.
                properties:
                  enabled:
                    default: false
                    description: If true, the controller will prefer (or require)
                      spot nodes.
                    type: boolean
                  maxRetries:
                    default: 3
                    description: Max retries after eviction before the job is marked
                      Failed.
                    format: int32
                    type: integer
                  mode:
                    default: Preferred
                    description: 'Required: must land on a spot node. Preferred: best-effort.'
                    enum:
                    - Required
                    - Preferred
                    type: string
                type: object
              type:
                default: Scala
                description: Application type. Affects how the driver command is constructed.
                enum:
                - Scala
                - Python
                - R
                type: string
            required:
            - mainApplicationFile
            type: object
          status:
            description: SparkJobStatus reflects observed state.
            properties:
              completionTime:
                description: When the job completed (Succeeded or Failed).
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              desiredExecutors:
                description: Number of executors the auto-tuner decided on.
                format: int32
                type: integer
              driverPod:
                description: Driver pod name (once created).
                type: string
              estimatedCostUSD:
                description: Estimated cost in USD accrued so far (driver + executors
                  * uptime).
                type: string
              phase:
                description: Phase is a coarse high-level status shared by both CRDs.
                enum:
                - Pending
                - Running
                - Checkpointing
                - Resuming
                - Succeeded
                - Failed
                type: string
              retries:
                description: Retry attempts consumed.
                format: int32
                type: integer
              runningExecutors:
                description: Number of executor pods currently running.
                format: int32
                type: integer
              startTime:
                description: When the job was started (driver first scheduled).
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
